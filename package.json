{
  "name": "cad_to_usd_eptravel",
  "version": "1.0.0",
  "description": "Chrome extension to convert CAD to USD on EPTravelCards balance page",
  "main": "dist/content.js",
  "scripts": {
    "compile": "tsc",
    "build": "npm run compile && if not exist dist mkdir dist && copy manifest.json dist\\",
    "watch": "tsc --watch",
    "package": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.3",
    "@types/chrome": "^0.0.260"
  },
  "private": true
}
